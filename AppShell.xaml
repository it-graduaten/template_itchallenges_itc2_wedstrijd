<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="ITC2Wedstrijd.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:ITC2Wedstrijd"
    xmlns:views="clr-namespace:ITC2Wedstrijd.Views"
    Shell.FlyoutBehavior="Flyout"
    Title="ITC2Wedstrijd">

    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />

    <ShellContent Title="Categoriën"
                        ContentTemplate="{DataTemplate views:CategoriePage}" />
    <ShellContent Title="Clubs"
                        ContentTemplate="{DataTemplate views:ClubPage}" />
    <ShellContent Title="Sporten"
                        ContentTemplate="{DataTemplate views:SportPage}" />
    <ShellContent Title="Spelers"
                        ContentTemplate="{DataTemplate views:SpelerPage}" />
    <ShellContent Title="Ploegen"
                        ContentTemplate="{DataTemplate views:PloegPage}" />

</Shell>
